node_modules/
.DS_Store
.env
.env.*
*.log
.claude/memory/
.claude/projects/
client_secret_*.json
token.json
vendor/
composer.phar
composer-setup.php

# Waiting-list capture — holds names and phone numbers.
# Normally lives ABOVE the web root; this is insurance in case the path
# is ever changed to somewhere inside the theme.
loc-slot-interest.csv
